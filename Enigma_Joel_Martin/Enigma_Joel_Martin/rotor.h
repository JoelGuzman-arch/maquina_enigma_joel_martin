#pragma once
#include <string>

// Cambiado para evitar referencias: Paso por valor
std::string leerRotor(std::string nombre_archivo);
std::string crearInverso(std::string rotor);
char toUpper(char caracter_entrada);
